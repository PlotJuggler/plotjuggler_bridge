Implement the following plan:

# Plan: Fix AppImage schema discovery (hybrid approach)

## Context

The AppImage fails at runtime with "Empty schema" for every topic. Two problems:

1. **`cp -r --parents` bug** (line 70): absolute paths cause `.msg` files to land at `AppDir/usr/share/home/runner/.pixi/.../sensor_msgs/msg/` instead of `AppDir/usr/share/sensor_msgs/msg/`
2. **`AMENT_PREFIX_PATH` overwrite** (line 93): the host's ament prefix is replaced entirely, so even if the user has a workspace sourced, custom message types are invisible

## Changes

### File: `.github/workflows/appimage.yaml`

**1. Fix the `.msg`/`.srv` copy loop** (lines 68-71) — replace `cp --parents` with correct relative copies:

```yaml
          # BEFORE (broken):
          for pkg_dir in "$CONDA_PREFIX"/share/*/msg "$CONDA_PREFIX"/share/*/srv; do
            [ -d "$pkg_dir" ] && cp -r --parents "$pkg_dir" AppDir/usr/share/ 2>/dev/null || true
          done

          # AFTER (correct):
          for pkg_dir in "$CONDA_PREFIX"/share/*/msg "$CONDA_PREFIX"/share/*/srv; do
            [ -d "$pkg_dir" ] || continue
            pkg_name="$(basename "$(dirname "$pkg_dir")")"
            mkdir -p "AppDir/usr/share/$pkg_name"
            cp -r "$pkg_dir" "AppDir/usr/share/$pkg_name/"
          done
```

This produces `AppDir/usr/share/sensor_msgs/msg/Float32.msg` — the path `ament_index_cpp` expects.

**2. Update AppRun** (line 93) — prepend bundled prefix instead of overwriting host `AMENT_PREFIX_PATH`:

```bash
# BEFORE:
export AMENT_PREFIX_PATH="${HERE}/usr"

# AFTER:
export AMENT_PREFIX_PATH="${HERE}/usr${AMENT_PREFIX_PATH:+:$AMENT_PREFIX_PATH}"
```

This way:
- Bundled standard msgs (std_msgs, sensor_msgs, geometry_msgs, etc.) work out of the box
- If user has sourced a workspace with custom msgs, those are also visible
- The bundled prefix takes priority (searched first)

## Verification

1. Push, trigger `workflow_dispatch` on AppImage workflow
2. Download the humble AppImage artifact
3. Test without sourcing ROS2 (standard msgs only):
   ```bash
   ./pj_bridge_ros2-humble-x86_64.AppImage --ros-args -p port:=8080
   ```
4. Test with sourced workspace (custom msgs):
   ```bash
   source ~/my_workspace/install/setup.bash
   ./pj_bridge_ros2-humble-x86_64.AppImage --ros-args -p port:=8080
   ```
5. Confirm no "Empty schema" errors for standard types in both cases


If you need specific details from before exiting plan mode (like exact code snippets, error messages, or content you generated), read the full transcript at: /home/davide/.REDACTED.jsonl

---

trigger appimage CI

---

download here the humble appimage

---

I could see the topics in t PJ client but then: [2026-03-10 16:22:24.247] [info] Returning 7 topics to client '0'
[2026-03-10 16:22:25.213] [info] Returning 7 topics to client '0'
[ERROR] [1773156145.982314471] [pj_bridge]: Failed to create subscription for topic '/nissan/vehicle_steering' (type 'std_msgs/msg/Float32'): Typesupport library for std_msgs does not exist in '/tmp/.mount_pj_briJfCInE/usr'.
[2026-03-10 16:22:25.982] [error] Failed to subscribe to topic '/nissan/vehicle_steering'
[ERROR] [1773156145.982514214] [pj_bridge]: Failed to create subscription for topic '/nissan/vehicle_speed' (type 'std_msgs/msg/Float32'): Typesupport library for std_msgs does not exist in '/tmp/.mount_pj_briJfCInE/usr'.
[2026-03-10 16:22:25.982] [error] Failed to subscribe to topic '/nissan/vehicle_speed'
[ERROR] [1773156145.982707002] [pj_bridge]: Failed to create subscription for topic '/nissan/gps/duro/status_string' (type 'std_msgs/msg/String'): Typesupport library for std_msgs does not exist in '/tmp/.mount_pj_briJfCInE/usr'.
[2026-03-10 16:22:25.982] [error] Failed to subscribe to topic '/nissan/gps/duro/status_string'
[2026-03-10 16:22:25.984] [info] Client '0' subscribed to topic '/nissan/gps/duro/mag' (type: sensor_msgs/msg/MagneticField)
[2026-03-10 16:22:25.984] [info] Client '0' subscribed to topic '/nissan/gps/duro/imu' (type: sensor_msgs/msg/Imu)
[ERROR] [1773156145.985238745] [pj_bridge]: Failed to create subscription for topic '/nissan/gps/duro/current_pose' (type 'geometry_msgs/msg/PoseStamped'): Typesupport library for geometry_msgs does not exist in '/tmp/.mount_pj_briJfCInE/usr'.
[2026-03-10 16:22:25.985] [error] Failed to subscribe to topic '/nissan/gps/duro/current_pose'
[ERROR] [1773156145.985461339] [pj_bridge]: Failed to create subscription for topic '/events/read_split' (type 'rosbag2_interfaces/msg/ReadSplitEvent'): Typesupport library for rosbag2_interfaces does not exist in '/tmp/.mount_pj_briJfCInE/usr'.
[2026-03-10 16:22:25.985] [error] Failed to subscribe to topic '/events/read_split'

---

[Request interrupted by user]

---

if we try to bundle them, it will be impossible to work with custome emssages that are not embedded into the AppImage